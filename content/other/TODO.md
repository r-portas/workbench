# TODO

> List of things to do

- [ ] ...
- [ ] Revisit moving `web/` into `apps/` (e.g. `apps/docs`) now that `apps/` exists — check the
      Vercel project's root directory setting before renaming, since it may be configured outside
      the repo
