{
  "name": "workbench",
  "private": true,
  "scripts": {
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "update": "bun update --interactive --latest",
    "update:all": "for d in . web templates/*; do (cd \"$d\" && bun update --interactive --latest); done"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "oxfmt": "^0.65.0"
  }
}
